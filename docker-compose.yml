services:
  web:
    build: .
    ports:
      - "3000:3000"
      - "3001:3001"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - DISABLE_NOTIFIER=true
      - CHOKIDAR_USEPOLLING=true
